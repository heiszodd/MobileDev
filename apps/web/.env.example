# GitHub OAuth — from github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# NextAuth — generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Google Gemini — from aistudio.google.com
GEMINI_API_KEY=

# Relay server — update after Fly.io deploy
NEXT_PUBLIC_RELAY_URL=ws://localhost:3001

# Supabase — from supabase.com project settings
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
